ALTER TABLE cli_exam_request
    ADD COLUMN IF NOT EXISTS is_critical SMALLINT NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS critical_acknowledged SMALLINT NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS critical_ack_by BIGINT,
    ADD COLUMN IF NOT EXISTS critical_ack_time TIMESTAMP,
    ADD COLUMN IF NOT EXISTS report_url VARCHAR(512);
