apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: his-infra
  labels:
    app: redis
    app.kubernetes.io/part-of: smart-his
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - name: redis
      protocol: TCP
      port: 6379
      targetPort: 6379
