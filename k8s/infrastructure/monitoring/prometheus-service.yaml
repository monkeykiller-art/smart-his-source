apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: his-infra
  labels:
    app: prometheus
    app.kubernetes.io/part-of: smart-his
spec:
  type: ClusterIP
  selector:
    app: prometheus
  ports:
    - name: http
      protocol: TCP
      port: 9090
      targetPort: 9090
