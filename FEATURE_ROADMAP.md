# Smart HIS 功能迭代计划

## 目标

在现有患者管理、挂号、接诊、病历、诊断、医嘱、收费和基础服务能力上，逐步补齐门诊闭环、药品与检验协同、权限审计和运营分析能力。每个阶段都应保持现有基础流程可用，并完成自动化测试和浏览器回归验证。

## 当前基线

- 患者档案、患者查询和患者详情
- 门诊挂号、排班、缴费状态和接诊状态流转
- 门诊病历草稿、签署、诊断和 ICD-10 检索
- 医嘱开立、提交收费、账单生成和收退款操作
- 网关、认证、患者、临床、运营服务及 Docker 本地环境
- 病历模板：普通感冒、高血压复诊、糖尿病复诊

## 实施顺序

### M4：门诊临床效率

1. **病历模板管理**
   - 将固定模板升级为可配置模板，支持新增、编辑、停用和按科室分类。
   - 增加常用短语，可插入主诉、现病史、查体和诊疗计划。
   - 验收：选择模板后字段正确填充；停用模板不再出现在新建病历中；已签署病历保持只读。

2. **病历自动保存与版本记录**
   - 草稿定时保存，记录修改人、修改时间和版本差异。
   - 支持恢复最近一次草稿，避免浏览器刷新造成内容丢失。
   - 验收：刷新页面后可恢复草稿；签署后版本不可覆盖；异常断网时有明确提示。

3. **患者查询体验统一**
   - 患者搜索同时支持系统数字 ID、EMPI、姓名、证件号和手机号。
   - 运营管理支持从 EMPI 解析到系统患者 ID，避免用户手工查找数字 ID。
   - 验收：使用任一合法标识都能定位同一患者；无匹配结果时显示可理解的提示。

### M5：检验、检查与处方闭环

1. **检验申请与结果回写**
   - 从医嘱生成检验申请，支持接收、执行、审核和结果回写病历。
   - 增加危急值标记和医生确认记录。
   - 验收：申请状态可完整流转；结果能在患者就诊记录中查看；危急值触发提醒。

2. **检查申请与报告管理**
   - 支持检查项目、检查状态、报告内容和附件链接。
   - 为后续 PACS 对接保留外部检查号和报告地址。
   - 验收：申请、执行、报告和取消状态可追踪，报告只允许授权人员修改。

3. **电子处方与用药校验**
   - 增加药品目录、剂量频次校验、过敏和重复用药提醒。
   - 支持处方审核、退回修改和打印。
   - 验收：缺少必要剂量或数量时不能提交；已审核处方不可直接修改。

### M6：药房与库存

1. 药品目录、价格、库存、批号和有效期管理。
2. 入库、出库、盘点、退药和报损。
3. 门诊发药、处方核对、库存不足和近效期提醒。
4. 验收：库存数量与业务单据一致；并发出库不会出现负库存；所有库存变化可追溯。

### M7：收费、医保与对账

1. 收费项目和价格配置、预交金账户及欠费管理。
2. 日结、收银员交班、对账和异常交易处理。
3. 医保目录映射及医保结算接口适配层。
4. 发票、收据和费用清单打印。
5. 验收：收款、退费、冲正和作废符合状态约束；金额精度统一为最多四位小数；对账结果可复核。

### M8：住院与急诊

1. 入院、转科、床位、出院和住院费用。
2. 急诊分级分诊、留观和抢救记录。
3. 手术申请、排班、麻醉和出院小结。
4. 验收：住院状态和费用状态一致；急诊优先级可排序；出院后关键资料只读归档。

### M9：权限、安全与运营分析

1. 角色、菜单、科室和数据范围权限。
2. 登录安全、二次认证、操作审计和敏感信息访问记录。
3. 门诊量、收入、退费、库存、医生工作量和科室绩效报表。
4. Excel/PDF 导出和报表查询条件保存。
5. 验收：不同角色只能看到授权数据；关键操作均有审计记录；报表统计与明细账单可勾稽。

## 每阶段交付要求

1. 先补充后端接口和业务规则，再接入前端页面。
2. 新增或修改后端逻辑时运行相关模块测试；涉及多模块时运行 Maven `clean verify`。
3. 前端改动运行 `npm test`、`npm run lint` 和 `npm run build`。
4. 涉及数据库时执行 Flyway 迁移验证，并检查升级和回滚路径。
5. 每个阶段至少完成一条正常流程、一条权限或状态限制流程和一条异常流程的浏览器验证。
6. 检查 `git status` 和差异内容，确认没有密钥、临时文件或无关修改后再创建独立 Git commit。

## 建议近期执行顺序

先完成 M4 的病历模板管理和患者查询统一，再做检验检查闭环。这样可以直接改善当前临床录入和运营查询问题，并为后续处方、库存和医保流程提供稳定的患者与就诊基础数据。
