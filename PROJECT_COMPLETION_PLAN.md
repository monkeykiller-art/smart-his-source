# Smart HIS 项目完成度分析与后续交付计划

> 适用仓库：`smart-his-source`  
> 盘点基线：2026-09-10  
> 范围：开发、测试、集成、安全、部署与交付

## 一、结论

当前项目已经完成微服务代码骨架、六个核心业务域的大部分接口与持久化代码，并建立了 Java 21、Maven Wrapper、Maven Enforcer、基础 GitHub Actions 和容器部署清单。项目可以继续在现有技术栈上开发，无需重新选型或重写。

目前不能把“已有大量 Java 类和接口”视为“系统已经完成”。数据库迁移尚未经过真实 PostgreSQL 自动验证，核心业务模块测试极少，跨服务主流程没有端到端测试，网关仍使用宽松开发配置，五个扩展模块主要是占位实现，前端仍是网关内的静态工作台。综合判断如下：

| 观察口径 | 估算完成度 | 判断 |
| --- | ---: | --- |
| 代码骨架与接口实现 | 55%—60% | 核心模块代码量较充足，扩展模块和前端明显不足 |
| 可测试、可联调的交付能力 | 25%—30% | 构建基线已改善，数据库、消息和跨服务验证仍缺失 |
| 生产可用能力 | 15%—20% | 安全、审计、可观测性、容量和发布演练尚未闭环 |

这些百分比用于排定工作顺序，不作为功能验收或项目结算依据。后续验收以自动测试、可重复部署和业务流程通过为准。

## 二、代码库盘点

### 2.1 规模与结构

盘点结果：

- 1 个父工程和 13 个子模块，共 14 个 Maven reactor 项目。
- 625 个主 Java 文件、4 个测试 Java 文件、18 个 Flyway SQL 文件。
- 12 个可执行服务：网关、认证、患者、临床、资源、运营、协同、药事、CDSS、DRG、急诊和平台。
- Docker Compose 提供 PostgreSQL、Redis、Kafka 和 Nacos；`k8s` 下已有基础设施与服务清单。
- 网关托管 `index.html`、`app.css` 和 `app.js`，尚无独立前端工程、包管理和组件测试。

各模块的静态代码分布如下。数量只能表示实现规模，不能证明业务正确性。

| 模块 | 主 Java | Controller | Service | Entity | Mapper | 测试类 | 迁移 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `his-auth` | 39 | 6 | 8 | 7 | 7 | 1 | 2 |
| `his-patient` | 99 | 10 | 20 | 12 | 12 | 1 | 3 |
| `his-clinical` | 126 | 7 | 22 | 14 | 14 | 0 | 2 |
| `his-resource` | 110 | 11 | 20 | 11 | 11 | 0 | 2 |
| `his-operations` | 104 | 7 | 14 | 12 | 12 | 0 | 2 |
| `his-pharma` | 84 | 8 | 14 | 8 | 8 | 0 | 2 |
| `his-collaboration` | 10 | 1 | 0 | 0 | 0 | 0 | 1 |
| `his-emergency` | 10 | 1 | 0 | 0 | 0 | 0 | 1 |
| `his-cdss` | 2 | 1 | 0 | 0 | 0 | 0 | 1 |
| `his-drg` | 2 | 1 | 0 | 0 | 0 | 0 | 1 |
| `his-platform` | 2 | 1 | 0 | 0 | 0 | 0 | 1 |
| `his-gateway` | 5 | 0 | 0 | 0 | 0 | 0 | 0 |
| `his-common` | 32 | 0 | 0 | 0 | 0 | 2 | 0 |

### 2.2 已经完成或基本完成

| 能力 | 当前结果 | 状态 |
| --- | --- | --- |
| 源码与版本管理 | Git 仓库已建立，原项目源码已导入并保留提交历史 | 已完成 |
| 工具链 | Java 21、Maven Wrapper 3.9.16、Wrapper 校验值和本地验证脚本已建立 | 基本完成 |
| 依赖治理 | Maven Enforcer 检查 Java/Maven 版本、依赖收敛、重复类和禁止依赖 | 基本完成 |
| 数据库驱动 | 达梦和金仓驱动已取消 `systemPath`，改为 Maven profile 管理 | 已完成 |
| 模块依赖 | Nacos、Actuator、Prometheus 等运行依赖已从父 POM 移到可执行服务 | 已完成 |
| Flyway 版本 | `his-patient` 的重复 V2 已改为唯一的 V1、V2、V3 顺序 | 已完成静态修复 |
| 持续集成 | Linux/Windows `clean verify` 和 PR 依赖审查工作流已添加 | 已建立，待线上连续验证 |
| 认证测试 | 登录、失败锁定、刷新、过期和撤销分支的 6 个单元测试已在本地通过 | 待提交 |
| 核心后端 | 患者、临床、资源、运营、药事包含较完整的 Controller/Service/Mapper/Entity | 已实现，缺系统验证 |

### 2.3 未完成和主要风险

| 优先级 | 未完成项 | 影响 |
| --- | --- | --- |
| P0 | 干净环境的全量 `clean verify` 尚未形成持续成功记录 | 无法证明任意机器可重复构建 |
| P0 | 18 个 Flyway 脚本没有真实 PostgreSQL 空库和升级测试 | 首次部署可能在建表、种子或方言处失败 |
| P0 | 625 个主类仅对应 4 个测试类 | 核心状态、金额、库存和医疗规则缺少回归保护 |
| P0 | 没有跨服务端到端测试 | 登录到发药的主业务链路无法自动验收 |
| P1 | 网关允许通配 CORS，Actuator 和 Swagger 位于白名单，开发 DEBUG 日志开启 | 配置误入生产会扩大攻击面并泄露细节 |
| P1 | JWT 缺少 issuer、audience、kid 和轮换机制 | 服务间令牌边界不清，密钥轮换困难 |
| P1 | API 与 Kafka 事件契约未版本化和集中验证 | 独立发布可能造成调用方不兼容 |
| P1 | `his-common` 同时承担 Web、数据、消息、FHIR 等能力 | 公共修改影响面大，依赖边界容易继续恶化 |
| P1 | 前端只有静态工作台 | 无完整登录态、动态路由、表单、错误处理和前端测试 |
| P2 | CDSS、DRG、平台、急诊和协同主要是占位代码 | 不能按生产业务模块验收 |
| P2 | 追踪、业务指标、告警和 Runbook 未闭环 | 跨服务故障难以定位和恢复 |
| P2 | 镜像扫描、SBOM、签名、容量测试和发布演练缺失 | 无法形成可审计的生产发布链路 |

## 三、完成原则

每项任务必须满足统一完成定义：

1. 代码、配置、迁移和文档已进入 Git。
2. 每次改动都有对应的自动测试或可重复验证脚本。
3. 模块测试和受影响的 reactor 校验全部通过。
4. 每项改动使用独立 Git commit，提交信息说明模块和行为。
5. 接口变更同步更新 OpenAPI、调用方和兼容性检查。
6. 数据变更只能新增 Flyway 迁移，不修改已经发布的迁移。
7. 安全敏感配置默认收紧，缺少生产密钥时启动失败。
8. 阶段验收失败时先修复基线，不继续堆叠新功能。

## 四、后续计划

建议按 16 周执行。人员增加时可以并行开发核心模块，但阶段门禁和依赖顺序保持不变。

### 阶段 R0：封板工程基线（第 1 周）

目标：让任何后续改动都有可信、可重复的构建结果。

任务：

- 提交已通过的认证测试，清理或确认所有未跟踪文件的归属。
- 在普通 Windows 终端和 Linux CI 各执行一次空缓存 `./mvnw clean verify`。
- 修复 Windows JDK 编译器文件句柄问题，验证不是源码或依赖损坏。
- 检查 GitHub Actions 实际运行结果，连续运行至少 20 次并记录成功率。
- 在 CI 添加 Surefire/Failsafe 报告摘要、失败日志和构建产物保留策略。
- 增加依赖漏洞检查和基础 secret scanning；高危问题阻止合并。
- 核对所有模块的 Dockerfile 能构建，但本阶段不发布镜像。

验收标准：

- 空 Maven 缓存的 Linux 与 Windows `clean verify` 一次通过。
- CI 连续 20 次无偶发失败。
- 工作区干净，所有改动都有可追踪提交。
- 高危依赖和凭据泄露检查成为 PR 门禁。

### 阶段 R1：数据库与核心服务测试（第 2—4 周）

目标：优先保护医疗、库存和费用等错误成本最高的规则。

任务顺序：

1. 使用 Testcontainers PostgreSQL 为全部 schema 增加空库迁移测试。
2. 为已有发布基线准备升级快照，增加“上一版本升级到最新版”测试。
3. `his-patient`：患者身份去重、挂号状态、入出院转换、FHIR R4 映射。
4. `his-clinical`：诊断主次约束、医嘱状态机、病历签名后禁止修改。
5. `his-resource`：库存并发扣减、床位占用、重复出入库和乐观锁。
6. `his-operations`：金额精度、押金、结算、重复支付和退费上限。
7. `his-pharma`：剂量、过敏、禁忌、相互作用和处方审核状态。
8. `his-auth`：补充角色越权、登出黑名单、真实 JWT 声明和 Redis 集成测试。

测试分层：领域单元测试使用 JUnit 5/AssertJ/Mockito；Mapper、事务和迁移使用 Testcontainers；Controller 使用 MockMvc 或 WebTestClient。

验收标准：

- 12 个有迁移的服务全部通过 PostgreSQL 空库迁移测试。
- 六个核心业务服务均有领域单元测试和数据库集成测试。
- 核心领域行覆盖率先达到 50%，同时检查关键条件的分支覆盖率。
- 测试数据可重复创建和清理，不依赖共享开发数据库。

### 阶段 R2：网关、安全、配置与契约（第 5—6 周）

目标：建立服务独立发布所需的接口与安全边界。

任务：

- 将 CORS 来源、方法和请求头改为显式环境配置，禁止通配来源与凭据组合。
- 生产环境仅公开 liveness/readiness；Swagger 仅在开发测试环境开启。
- 生产关闭 DEBUG 日志，增加敏感字段脱敏测试。
- JWT 增加 issuer、audience、kid；实现双密钥轮换和严格声明校验。
- 区分用户令牌与服务身份，内部服务不信任未经签名的用户头。
- 为认证、患者读取、病历修改、处方、退费和权限变更建立审计事件。
- 导出所有 Controller 的 OpenAPI JSON，统一分页、金额、时间和错误响应格式。
- 在 CI 对 OpenAPI 和 Kafka schema 执行破坏性变更检测。
- 将配置改为 `@ConfigurationProperties` 并加入绑定和缺失值测试。

验收标准：

- 网关和服务内部同时验证权限，越权 API 测试全部通过。
- 生产配置缺少密钥时启动失败。
- OpenAPI 或事件契约发生破坏性变化时 CI 自动失败。
- 日志测试证明 Token、身份证号、手机号和数据库密码不会明文输出。

### 阶段 R3：首条完整业务流程与可用前端（第 7—9 周）

目标：交付一条用户可操作、可自动验收的门诊主流程。

后端流程：

```text
用户登录
→ 患者建档
→ 挂号
→ 诊断
→ 开立处方
→ 处方审核
→ 收费结算
→ 发药
```

任务：

- 建立稳定的端到端测试数据工厂和唯一业务编号。
- 明确每一步接口、状态、幂等键、补偿行为和审计记录。
- 使用 REST Assured 或 Newman 实现主流程与关键失败路径。
- 建立 React + TypeScript + Vite 前端工程；接入登录、令牌刷新和统一错误处理。
- 实现工作台布局、基于角色的菜单与路由守卫，以及患者、挂号、诊疗、收费和发药的最小页面。
- 前端使用 Vitest 测试鉴权、请求封装和关键表单；使用 Playwright 覆盖主流程。
- 生产部署使用 Nginx 托管前端静态制品并代理网关 API。

验收标准：

- 新环境从基础设施启动到首条流程通过只需一条文档化命令。
- 正常流程、重复提交、权限不足和下游失败均有自动测试。
- 不同角色只能看到和访问获授权功能。
- 前端 lint、类型检查、单元测试和生产构建全部进入 CI。

### 阶段 R4：服务边界与五个扩展模块（第 10—13 周）

目标：减少公共耦合，并让占位模块各自形成一条生产级纵切片。

先执行公共模块治理：

- 生成 Maven 模块依赖图、数据库 schema 清单和 API 清单。
- 用 ArchUnit 固化 Controller、application、domain、infrastructure 的依赖方向。
- 依次抽取 `common-core`、`common-web`、`common-security`、`common-observability` 和 `fhir-support`。
- 禁止跨服务共享 Entity、Mapper 或直接访问其他 schema。
- Kafka 使用统一事件信封；生产端使用 Outbox，消费端使用 Inbox 或去重表。

扩展模块按依赖顺序交付：

| 顺序 | 模块 | 第一条纵切片 | 必须验证的规则 |
| ---: | --- | --- | --- |
| 1 | `his-platform` | 术语导入、版本发布、编码查询 | 版本不可变、FHIR 术语校验、审计 |
| 2 | `his-cdss` | 药物过敏决策 | 规则版本、优先级、提示/警告/拦截结果 |
| 3 | `his-emergency` | 分诊到抢救状态流转 | 非法转换、重复命令、并发版本冲突 |
| 4 | `his-collaboration` | 会诊申请到完成 | 权限、超时、撤销、领域事件 |
| 5 | `his-drg` | 病案分组与结果保存 | 分组器适配、规则版本、输入快照、可复算 |

验收标准：

- 五个模块各有迁移、领域规则、API、权限、审计、指标和四层测试。
- 服务之间不存在实现级数据共享。
- 重复 Kafka 消息不会重复产生业务副作用。
- 跨服务流程失败后可自动重试、补偿或人工恢复。

### 阶段 R5：可观测性、性能与发布（第 14—16 周）

目标：形成可诊断、可度量、可回滚的生产交付链路。

任务：

- 接入 Micrometer Tracing 与 OpenTelemetry，统一传播 HTTP、Feign 和 Kafka trace。
- 使用结构化 JSON 日志并统一 traceId、spanId、脱敏用户标识和业务流水号。
- 建立 RED 指标及挂号、处方审核、库存、结算等业务指标。
- 为关键告警编写 Runbook，并演练数据库、Redis、Kafka 和下游服务故障。
- 用 k6 或 Gatling 建立固定数据集与容量模型；定位慢 SQL 和缺失索引。
- 构建带 Git SHA 的独立服务镜像，生成 SBOM，执行漏洞扫描和签名。
- 使用 Helm 或 Kustomize 管理环境差异，执行滚动或金丝雀发布。
- 完成数据库备份恢复、应用回滚和前向迁移修复演练。

验收标准：

- 核心查询 P95 小于 500ms，核心写入 P95 小于 1s；最终阈值按容量测试修订。
- 任一主流程请求可从网关追踪到数据库和 Kafka 消费端。
- 制品可以追溯到源码 SHA、依赖清单、测试报告和扫描结果。
- 测试环境自动部署、烟雾测试、回滚和恢复演练全部通过。

## 五、里程碑与资源建议

| 里程碑 | 时间 | 结果 |
| --- | --- | --- |
| M1 工程基线 | 第 1 周末 | 干净构建、稳定 CI、安全扫描 |
| M2 核心可信 | 第 4 周末 | 全 schema 迁移测试、六个核心模块测试 |
| M3 可联调版本 | 第 6 周末 | 网关安全、配置和接口契约受 CI 保护 |
| M4 可演示版本 | 第 9 周末 | 门诊主流程和最小前端自动通过 |
| M5 功能完整 | 第 13 周末 | 五个扩展模块完成首条生产纵切片 |
| M6 候选发布 | 第 16 周末 | 可观测、性能、制品和回滚闭环 |

最低建议团队配置：2 名后端、1 名测试开发、1 名前端；架构、安全和运维可由有经验成员兼职评审。若只有 1 名开发者，应保持任务顺序，将总周期调整为 24—30 周，不压缩测试和安全门禁。

## 六、近期两周任务清单

按以下顺序执行，前一项验收通过后再开始后一项：

1. 提交认证模块 6 个测试并恢复干净工作区。
2. 让 Linux 与 Windows CI 的全量 `clean verify` 实际通过。
3. 添加全 schema PostgreSQL 空库迁移测试。
4. 添加 `his-patient` PostgreSQL 与 FHIR 集成测试。
5. 收紧网关 CORS、Actuator、Swagger 和日志配置。
6. 生成模块依赖图、schema 清单和 API 清单，作为后续边界治理基线。

两周结束时必须能展示：一个全绿的 CI 运行、全部 schema 的迁移报告、认证与患者测试报告、网关安全配置测试，以及与每项改动一一对应的 Git commit。

## 七、进度报告模板

每完成一步使用以下格式汇报：

```text
任务：
完成内容：
测试与验证：
Commit：
剩余风险：
下一步：
```

项目完成的最终标准是：核心和扩展模块都有真实数据验证，首条跨服务流程可在 CI 自动运行，前端可以完成该流程，生产配置默认安全，系统故障可定位，发布制品可追溯并可回滚。
