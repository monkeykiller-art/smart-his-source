# Smart HIS 项目实施与完成计划

> 仓库：`smart-his-source`
>
> 评估基线：2026-09-14
> 目标：先形成可演示、可回归的门诊 MVP，再完善安全、扩展业务和生产交付能力。

## 一、当前项目评估

### 1.1 工程规模

| 项目 | 当前情况 |
| --- | --- |
| 后端工程 | 15 个 Maven reactor 工程，包含 12 个可执行服务和 `his-migration-tests` |
| 后端源码 | 625 个主 Java 类 |
| 数据库 | 11 个业务 schema、18 个 Flyway 迁移脚本 |
| 后端测试 | 8 个测试类，覆盖构建规则、SQL 可移植性、认证、患者映射和数据库迁移；核心业务覆盖仍偏低 |
| 前端工程 | React 18、TypeScript、Vite、Ant Design、Zustand、TanStack Query |
| 前端页面 | 登录、工作台、患者管理、门诊挂号、临床接诊已实现；资源、运营、药事仍为占位页 |
| 前端测试 | 9 个测试文件、23 项测试；临床接诊最新改动尚未提交 |
| 自动化流水线 | GitHub Actions 已运行 Linux/Windows 后端验证和依赖审查，尚未纳入前端检查与端到端测试 |
| Git 状态 | `main` 比 `origin/main` 超前 23 个提交；存在已暂存文档和未提交临床前端改动 |

模块范围包括 `his-auth`、`his-gateway`、`his-patient`、`his-clinical`、`his-resource`、`his-operations`、`his-pharma`、`his-collaboration`、`his-cdss`、`his-drg`、`his-emergency` 和 `his-platform`。

### 1.2 已验证能力

- Java 21 和 Maven 3.9.16 工具链可用。
- 后端曾在普通终端完成全 reactor `clean verify`。
- PostgreSQL 16 Testcontainers 已真实验证 11 个 schema 和 18 个 Flyway 脚本。
- 2026-09-14 最近一次 `his-common`、`his-patient`、`his-clinical` 模块测试为 `BUILD SUCCESS`。
- 前端最近一次 `npm test` 为 9 个测试文件、23 项测试全部通过。
- 前端 `npm run lint` 和 `npm run build` 通过。
- 患者建档、资料编辑、排班查询、挂号、缴费、取消、退费退号和临床接诊已有对应前端源码。
- 临床工作台支持开始接诊、病历草稿、病历签署、诊断、ICD-10 检索、医嘱和结束接诊规则。

### 1.3 当前阻塞与主要缺口

1. Docker Desktop 程序可以打开，但当前 Docker 引擎未能从 Agent 环境连接；项目根目录也缺少本地 `.env`。
2. 登录到接诊结束的真实业务流程尚未在完整服务环境中走通。
3. `his-resource`、`his-operations`、`his-pharma` 后端接口较丰富，但前端仍是占位页面。
4. 625 个主 Java 类仅有 8 个测试类，号源并发、费用精度、库存扣减、处方审核等高风险规则缺少充分保护。
5. 前端菜单目前是固定配置，缺少动态 RBAC、按钮权限、403 页面和角色验收。
6. CI 没有运行前端测试、Lint、构建和浏览器端到端测试。
7. 前端公共依赖分块约 657.69 kB，生产构建存在体积提示。
8. `his-platform`、`his-cdss`、`his-drg` 主要是健康检查占位；`his-emergency`、`his-collaboration` 只有有限纵向能力。
9. OpenAPI 契约、Kafka 事件版本、审计、链路追踪、容量基线和发布回滚尚未形成闭环。

### 1.4 当前完成度估算

| 维度 | 估算 | 判断依据 |
| --- | ---: | --- |
| 工程与构建基线 | 80% | 工具链、模块、迁移测试和 CI 骨架已建立，完整本地启动仍待复核 |
| 核心后端接口 | 65% | 患者、临床、资源、运营和药事接口较完整，测试覆盖不足 |
| 门诊前端 | 65% | 登录、患者、挂号、接诊已实现，尚未完成真实联调 |
| 门诊业务闭环 | 45% | 收费、处方审核、库存与发药页面缺失 |
| 安全与权限 | 35% | 认证和 Token 已有，动态授权、审计验收和安全基线不足 |
| 部署与运维 | 30% | Compose、Kubernetes 和脚本已有，缺少稳定启动、监控和回滚演练 |
| 综合可交付程度 | 50% | 适合继续开发和局部演示，尚未达到医院试运行或生产上线条件 |

以上百分比用于排序，不作为验收结论。每个阶段必须以可重复测试和可操作流程为准。

## 二、项目范围与优先级

### 必须完成：门诊 MVP

- 用户登录和会话续期。
- 患者建档、查询和资料维护。
- 排班、挂号、缴费、取消及退费退号。
- 候诊、开始接诊、病历、诊断、医嘱和结束接诊。
- 收费明细、处方审核、库存扣减和发药。
- 角色权限、审计日志、异常处理和端到端测试。

### 应当完成：可交付基线

- 前后端 CI、依赖与敏感信息扫描。
- OpenAPI 契约、统一错误码和事件契约。
- 日志、指标、链路追踪、备份恢复和回滚演练。
- 性能基线、数据脱敏和基础安全测试。

### 后续迭代

- 住院业务的完整闭环。
- 急诊分级、绿色通道和抢救记录。
- CDSS、DRG、协同和平台主数据的生产级能力。
- 信创数据库专项适配和医院外部系统集成。

## 三、统一完成定义

每项任务同时满足以下条件才算完成：

1. 功能源码、配置或迁移已完成，改动范围清晰。
2. 新增或更新了相关单元测试、集成测试、契约测试或端到端测试。
3. 受影响模块检查全部通过；跨模块改动执行全量 `clean verify`。
4. 页面具备加载、空数据、失败、无权限和关键边界状态。
5. 写操作具备输入校验、权限、幂等或重复操作保护，并保留必要审计信息。
6. 数据库变更通过 PostgreSQL 16 Flyway 迁移验证，且不修改已发布迁移。
7. Agent 自动检查和用户实际流程检查都有明确结果。
8. 每个改动建立独立 Git commit，提交后工作区状态可解释、可回滚。

## 四、阶段安排

### P0：清理基线并恢复完整运行环境（1—2 天）

**目标：** 获得干净、可重复、能够启动完整系统的开发基线。

任务：

1. 提交临床接诊工作台改动，不混入指南和计划文档。
2. 整理当前已暂存文档，决定分别提交或取消暂存。
3. 将本地 23 个提交推送或备份到远端开发分支，避免本机单点丢失。
4. 从 `.env.example` 生成仅本地使用的 `.env`，使用随机密钥并确保被 `.gitignore` 排除。
5. 以管理员权限启动 Docker Desktop，确认 WSL 2 和 Docker Engine 可用。
6. 启动 PostgreSQL、Nacos、Redis、Kafka，检查所有容器健康状态。
7. 执行后端全量 `clean verify`、前端测试、Lint 和生产构建。

**验收标准：**

- `docker info` 成功，4 个基础设施容器健康。
- 后端全量 `clean verify` 一次通过，数据库迁移测试没有跳过。
- 前端 23 项及后续新增测试、Lint、构建全部通过。
- `.env`、密码、Token、日志和临时文件未进入 Git。
- 临床改动和计划文档各自拥有独立提交。

### P1：门诊核心流程真实联调（4—6 天）

**目标：** 走通登录、建档、挂号、接诊和医嘱的第一条真实流程。

任务：

1. 准备本地管理员、医生、收费员和药师测试账号。
2. 准备科室、医生、排班、ICD-10、药品和收费项目基础数据。
3. 验证患者建档、重复患者识别和资料修改。
4. 验证号源查询、挂号、缴费、退费、取消和并发余量。
5. 验证开始接诊、病历草稿编辑、签署、诊断、医嘱和结束接诊。
6. 核对状态刷新、错误提示、重复点击和服务暂时不可用场景。
7. 修复接口字段、状态机和跨服务调用中的实际问题。

**验收标准：**

- 门诊流程可以在浏览器中连续完成，刷新页面后状态保持一致。
- 同一号源并发挂号不会超卖，同一操作重复提交不会产生重复记录。
- 病历签署后不能修改，接诊结束后工作台只读。
- 关键失败场景有中文提示，日志可定位请求和服务。

### P2：收费与运营前端（4—6 天）

**目标：** 用真实页面替换 `operations` 占位页，形成费用闭环。

任务：

1. 实现患者待缴账单、费用明细和状态筛选。
2. 实现支付确认、退费、作废和重复操作保护。
3. 展示挂号费、药费、检验费和检查费来源。
4. 增加日结、结算和账户流水的基础页面。
5. 覆盖金额精度、退费上限、重复支付和并发状态测试。

**验收标准：**

- 挂号和医嘱产生的费用可查询、支付和退费。
- 页面金额与数据库金额一致，统一使用精确小数类型。
- 已支付账单不可重复支付，退款不得超过已付金额。

### P3：药事、库存与发药闭环（5—8 天）

**目标：** 用真实页面替换 `pharma` 和核心 `resource` 占位能力。

任务：

1. 实现待审核处方、审核通过、驳回和审核意见。
2. 展示剂量上限、过敏、禁忌和药物相互作用提示。
3. 实现药品、药房、库存和库存流水查询。
4. 实现待发药、确认发药和退药。
5. 将处方审核、收费、库存扣减和发药串联。
6. 增加库存并发扣减、重复发药和库存不足测试。

**验收标准：**

- 医生开出的药品医嘱可进入药师审核和发药队列。
- 严重用药风险必须阻止或要求明确处理。
- 发药只执行一次，库存不足时不产生负库存。
- 处方、账单、库存和发药状态最终一致。

### P4：权限、安全与审计（4—6 天）

**目标：** 让不同岗位只能查看和执行获授权的操作。

任务：

1. 根据后端权限生成动态菜单和按钮权限。
2. 增加角色路由守卫、403 页面和越权接口测试。
3. 校验 JWT 声明、刷新、撤销、登录锁定和登出流程。
4. 对患者身份、联系方式和医疗记录持续脱敏。
5. 为病历签署、缴费、退费、审核、发药等操作保留审计日志。
6. 收紧 CORS、Actuator、Swagger 和生产日志配置。

**验收标准：**

- 医生、药师、收费员和管理员看到的菜单及按钮不同。
- 隐藏按钮后直接调用接口仍会返回 403。
- 密码、Token、身份证和医疗敏感信息不出现在日志或 Git 中。
- 关键业务操作可以追溯操作人、时间和结果。

### P5：自动化测试与持续集成（5—8 天）

**目标：** 让主流程和关键失败路径在每次提交后自动回归。

任务：

1. 将前端 `npm test`、`npm run lint` 和 `npm run build` 加入 GitHub Actions。
2. 为患者去重、号源并发、病历签署、费用和库存状态机增加后端测试。
3. 用 Testcontainers 继续覆盖 PostgreSQL 真实事务和并发行为。
4. 增加登录到发药的浏览器端到端测试。
5. 增加 OpenAPI 兼容性和 Kafka 事件契约测试。
6. 上传测试报告、覆盖率和失败日志作为 CI 制品。

**验收标准：**

- Linux 和 Windows CI 同时验证后端与前端。
- 门诊主流程及关键失败路径可以自动回归。
- 失败测试能明确定位到模块、接口和业务步骤。
- 高危依赖漏洞或密钥泄露会阻止合并。

### P6：性能、可观测与部署验收（5—7 天）

**目标：** 形成可诊断、可部署、可回滚的候选版本。

任务：

1. 优化前端 657.69 kB 公共分块，建立页面懒加载和体积预算。
2. 为网关、Feign、数据库和 Kafka 接入统一请求标识和 OpenTelemetry。
3. 建立服务健康、错误率、延迟、数据库连接池和消息积压指标。
4. 验证 Docker Compose 和 Kubernetes 配置、资源限制及健康检查。
5. 执行核心接口容量测试和 8 小时稳定性测试。
6. 完成数据库备份恢复、灰度发布和回滚演练。

**验收标准：**

- 核心查询 P95 小于 500ms，核心写入 P95 小于 1s，且无数据错误。
- 任一主流程请求可以从网关追踪到数据库和消息消费者。
- 部署失败可以自动停止，候选版本可以按文档回滚。
- 备份数据可以在隔离环境恢复并通过一致性检查。

### P7：扩展业务模块（按产品优先级分批实施，4—10 周）

**目标：** 在门诊 MVP 稳定后逐步完成其他业务域。

任务：

1. `his-emergency`：急诊分级、队列、绿色通道和抢救记录。
2. `his-collaboration`：会诊、转诊、通知和闭环反馈。
3. `his-cdss`：规则、提醒、处置和命中效果统计。
4. `his-drg`：病案分组、费用分析和异常提示。
5. `his-platform`：字典、主数据、机构、参数和系统管理。
6. 住院业务：入院、床位、医嘱、费用、出院和结算闭环。

**验收标准：**

- 每个模块至少完成一条前端、后端、数据库、权限和测试齐全的纵向流程。
- 扩展模块不能破坏已验收的门诊主流程。
- 产品负责人按医院实际优先级逐项验收，未验收模块不得标记完成。

## 五、里程碑与预计工期

| 里程碑 | 建议时间 | 可交付结果 |
| --- | --- | --- |
| M0 基线恢复 | 第 1—2 天 | Git 清晰、Docker 可用、全量检查通过 |
| M1 门诊联调 | 第 1 周末 | 登录到接诊结束的真实流程 |
| M2 收费闭环 | 第 2 周末 | 账单、支付、退费和结算页面 |
| M3 药事闭环 | 第 3—4 周 | 处方审核、库存、收费和发药闭环 |
| M4 权限安全 | 第 5 周 | RBAC、403、审计和安全配置 |
| M5 自动回归 | 第 6—7 周 | 前后端 CI、数据库与浏览器端到端测试 |
| M6 候选版本 | 第 8 周 | 性能、监控、部署、备份与回滚通过 |
| M7 扩展模块 | 第 9 周以后 | 按优先级逐步交付急诊、协同、CDSS、DRG 和平台能力 |

两名后端、一名前端和一名测试开发可按上述 8 周完成门诊候选版本。单人配合 Agents 开发时，建议按同一顺序执行，将门诊候选版本调整为约 14—20 周；实际工期取决于接口缺陷数量、测试数据和医院业务规则确认速度。

## 六、任务执行与提交规则

每个任务使用以下步骤：

1. Agent 阅读相关接口、数据库和现有测试。
2. Agent 修改源码并补充测试。
3. Agent 自动执行受影响模块测试和静态检查。
4. 用户在普通终端或浏览器完成真实环境检查。
5. Agent 根据结果继续修复，直到适用验证全部通过。
6. 只暂存本任务文件，检查 `git diff --staged`。
7. 创建一个独立 Git commit，并用 `git status`、`git log -1 --oneline` 复核。

每次汇报统一采用：

```text
任务：
完成内容：
Agent 自动检查：
用户实际检查：
Commit：
剩余风险：
下一步：
```

## 七、用户配合事项与电脑影响

用户通常只需要完成以下工作：

- 出现 Windows 管理员确认时，决定是否允许 Docker Desktop 启动服务。
- 在普通 PowerShell 中运行 Agent 提供的验证或 Git 提交命令。
- 在浏览器按清单检查真实业务流程和页面显示。
- 对医院实际业务规则、角色权限和验收结果作出确认。

资源影响：

- 前端测试和 Maven 测试只会短时使用 CPU、内存和项目缓存。
- Docker 同时运行 PostgreSQL、Nacos、Redis 和 Kafka 时会明显占用内存，测试结束后应执行 `docker compose stop` 并退出 Docker Desktop。
- 不执行 `docker compose down -v`，除非明确需要删除本地测试数据库。
- `.env` 只保存在本机且不提交；任何命令都不得覆盖用户个人文件或修改无关系统配置。

## 八、下一批立即执行的任务

1. 用户完成临床接诊页面实际检查并提交对应源码。
2. 以管理员权限启动 Docker Desktop，确认 `docker info` 成功。
3. 安全创建本地 `.env`，启动并检查四个基础设施容器。
4. 执行带 PostgreSQL 迁移测试的全量 `clean verify`。
5. 启动网关、认证、患者和临床服务，完成第一条门诊真实流程。
6. 修复联调发现的问题并增加回归测试。
7. 开始实现收费与运营页面。

## 九、项目最终完成标准

- 登录、患者、挂号、接诊、诊断、处方、收费、审核、库存和发药能够完整操作。
- 核心成功路径和失败路径在 CI 中自动通过。
- 11 个 schema 和所有 Flyway 迁移在 PostgreSQL 16 中可重复验证。
- 权限、审计、脱敏和生产安全配置经过角色与接口双重验收。
- 前后端能在干净 Windows 和 Linux 环境重复构建。
- 系统具备日志、指标和链路追踪，故障能够定位。
- 发布制品可追溯到 Git commit、依赖、测试和扫描结果。
- 部署、备份、恢复和回滚均完成演练。
- 门诊 MVP 未通过上述标准前，不进入医院试运行或生产上线。
