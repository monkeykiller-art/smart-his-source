# Smart HIS 基础门诊版实施计划

> 计划基线：2026-09-15
> 目标：只交付一条能够重复演示和自动回归的基础门诊流程。

## 一、交付范围

基础门诊版只保留以下能力：

1. 登录、会话续期和退出。
2. 患者建档、查询和资料修改。
3. 科室、医生、排班查询，以及挂号、缴费、取消和退费退号。
4. 候诊患者选择、开始接诊、病历、诊断、医嘱和结束接诊。
5. 门诊账单查询、费用明细、收款、退费和作废。

保留的 Maven 工程为：

| 工程 | 职责 |
| --- | --- |
| `his-common` | 通用响应、上下文、安全和基础组件 |
| `his-gateway` | 统一入口、鉴权头传递和静态资源 |
| `his-auth` | 登录、令牌刷新和用户信息 |
| `his-patient` | 患者、科室、医生、排班、挂号和接诊记录 |
| `his-clinical` | 病历、诊断、ICD-10 和医嘱 |
| `his-operations` | 门诊账单、明细、收款、退费和作废 |
| `his-migration-tests` | PostgreSQL 16 迁移验证 |
| `smart-his-frontend` | 基础门诊浏览器工作台 |

不纳入当前版本：药事、药房库存、床位、住院结算、急诊、会诊协同、CDSS、DRG 和平台管理。对应独立服务和前端入口从当前源码中删除，如以后确有需求，应从 Git 历史恢复后单独规划。

## 二、统一完成条件

每个功能必须同时满足：

1. 正常流程可以在浏览器连续完成，刷新后状态正确。
2. 输入错误、重复操作、无权限和服务异常有明确中文提示。
3. 写操作具备事务、幂等或状态保护，不产生重复业务记录。
4. 后端相关模块测试、前端 `npm test`、`npm run lint` 和 `npm run build` 全部通过。
5. PostgreSQL 16 Flyway 迁移测试真实执行且没有跳过。
6. 改动不包含密码、Token、日志、临时文件和无关文档。
7. 每项改动创建独立 Git commit，提交后工作区可解释、可回退。

## 三、实施阶段

### M0：精简和稳定启动

任务：

1. 删除扩展服务、网关路由、菜单、占位页面和相关测试。
2. 启动脚本只构建并启动 5 个可执行服务。
3. 工作台只监控认证、患者、临床和运营服务。
4. 建立最小测试账号和可重复的开发数据初始化方式。

验收标准：

- Maven reactor 只包含基础门诊模块。
- Docker 基础设施保持健康，5 个应用服务可以一次启动成功。
- 工作台显示 4/4 个业务服务运行中。

### M1：患者与挂号闭环

任务：

1. 完成患者建档、搜索、编辑和重复身份识别。
2. 准备科室、医生和当日排班测试数据。
3. 完成号源查询、挂号、挂号缴费、取消和退费退号。
4. 增加号源余量、重复提交和并发防超卖测试。

验收标准：

- 新患者可以完成建档并在刷新后查询到。
- 已开放排班可以挂号，余量准确递减且不会超卖。
- 取消和退费后状态、号源及费用一致。

### M2：临床接诊闭环

任务：

1. 已缴费挂号进入候诊列表并可开始接诊。
2. 完成病历草稿、更新、签署和只读保护。
3. 完成 ICD-10 检索、诊断维护和门诊医嘱。
4. 完成结束接诊及刷新后的状态恢复。

验收标准：

- 挂号患者能够连续完成接诊、病历、诊断、医嘱和结束接诊。
- 病历签署后不能修改，结束接诊后工作区只读。
- 重复点击不会重复创建病历、诊断或医嘱。

### M3：门诊收费闭环

任务：

1. 挂号和医嘱生成的费用进入患者账单。
2. 展示账单明细、来源、应收、已收和待收金额。
3. 完成线下收款、部分收款、退费和未收款账单作废。
4. 增加金额精度、重复支付、退款上限和并发状态测试。

验收标准：

- 页面与数据库金额一致，统一使用精确小数。
- 已结清账单不能重复收费，累计退款不能超过已收金额。
- 收费结果能够正确回写挂号及门诊流程状态。

### M4：自动回归和交付

任务：

1. 增加“登录 → 建档 → 挂号 → 接诊 → 收费”的浏览器端到端测试。
2. 在 CI 中执行后端验证、前端测试、Lint 和生产构建。
3. 检查权限、错误提示、日志脱敏和关键操作审计。
4. 完成启动、停止、备份和 Git 回退说明。

验收标准：

- 全流程可以重复执行，失败能够定位到具体步骤和服务。
- Linux 与 Windows 自动检查全部通过。
- 新环境按照 README 可以完成构建、启动和浏览器验收。

## 四、执行顺序

当前先完成 M0，然后严格按照 M1、M2、M3、M4 顺序实施。未满足本阶段验收标准前，不开始新的业务域，也不重新加入已删除的扩展模块。

每个阶段结束时执行：

```powershell
.\mvnw.cmd clean verify
Set-Location smart-his-frontend
npm test
npm run lint
npm run build
Set-Location ..
git status --short
git log -1 --oneline
```

数据库迁移验证必须在 Docker Desktop 和 PostgreSQL 16 Testcontainers 可用时执行，不得以跳过测试作为完成依据。
